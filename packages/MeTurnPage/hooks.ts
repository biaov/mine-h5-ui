import { ref, watch } from 'vue'
import { FormatData } from '../MeAPI/types'
import type { Props, Emits } from './types'

/**
 * 倒计时
 */
export const useTurnPage = (props: Readonly<Required<Props>>, emit: Emits) => {
  return {}
}
