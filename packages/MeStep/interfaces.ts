/* eslint-disable no-unused-vars */
// props
export interface Props {
  active: (string | number)[]
  direction: string
  type: string
  color: string
  activeColor: string
  icon: string
  activeIcon: string
}
