import { PublicProps } from '../types';
export declare type Props = PublicProps<{
    modelValue: boolean;
    size: string;
    activeColor: string;
    inactiveColor: string;
    async: boolean;
    disabled: boolean;
}>;
