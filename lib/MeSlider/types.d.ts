import { PublicProps } from '../types';
import { PropStyles } from './interfaces';
export declare type Props = PublicProps<{
    modelValue: number;
    max: number;
    min: number;
    styles: PropStyles;
    disabled: boolean;
    isBtn: boolean;
}>;
