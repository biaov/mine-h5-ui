import { PublicProps } from '../types';
export declare type Props = PublicProps<{
    modelValue: boolean;
    startValue: number;
    endValue: number;
    duration: number;
    thousand: boolean;
}>;
