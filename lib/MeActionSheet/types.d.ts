import { PublicProps } from '../types';
import { ListItem } from './interfaces';
export declare type Props = PublicProps<{
    visible: boolean;
    list: ListItem[];
    index: string;
    label: string;
}>;
