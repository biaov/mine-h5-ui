import { PublicProps } from '../types';
export declare type Props = PublicProps<{
    time: number;
    format: string;
    isStart: boolean;
    isSuspend: boolean;
    isReset: boolean;
}>;
