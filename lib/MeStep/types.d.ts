import { PublicProps } from '../types';
export declare type Props = PublicProps<{
    active: (string | number)[];
    direction: string;
    type: string;
    color: string;
    activeColor: string;
    icon: string;
    activeIcon: string;
}>;
